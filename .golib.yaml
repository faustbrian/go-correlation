schema_version: 1
tool_version: v1.0.13
tool_checksums_sha256: 164e5c8abefe655958f522677856816faddbd2a8d40eba7d37ba5a031210df70
manifest:
  modules: modules.json
  packages: packages.json
evidence:
  root: ".verification"
mutation:
  root: ".verification/mutation"
  imports:
  - module: "."
    archive: ".verification/mutation/bootstrap/root.zip"
    ledger: ".verification/mutation/migration-ledger.json"
api:
  baselines:
  - module: "."
    mode: go-doc
    path: api/baseline.txt
    packages:
    - "."
    - ./http
    - ./http/requestidbridge
    - ./jsonrpc
    - ./log
    - ./queue
    - ./schedule
    - ./telemetry
    - ./webhook
operations:
- module: "."
  gate: fuzz
  steps:
  - type: go-test
    packages: ["."]
    fuzz: '^FuzzParseCorrelationID$'
    budget: 2s
    timeout: 2m
  - type: go-test
    packages: ["."]
    fuzz: '^FuzzCarrierExtraction$'
    budget: 2s
    timeout: 2m
  - type: go-test
    packages: ["./http"]
    fuzz: '^FuzzInboundHeaders$'
    budget: 2s
    timeout: 2m
  - type: go-test
    packages: ["./jsonrpc"]
    fuzz: '^FuzzCustomMetadataCarrier$'
    budget: 2s
    timeout: 2m
- module: "."
  gate: docs
  steps:
  - type: go-test
    packages: ["./..."]
    run: '^Example'
    timeout: 20m
- module: "."
  gate: benchmark
  steps:
  - type: go-test
    packages: ["."]
    benchmark: Benchmark
    budget: 100ms
    timeout: 20m
